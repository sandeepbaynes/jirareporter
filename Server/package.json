{
  "name": "jira-reporter-svr",
  "version": "1.0.0",
  "description": "Server side API to get data from Jira API",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=dev nodemon server.js --ignore ./scripts/ -unhandled-rejections=strict",
    "prod": "NODE_ENV=prod nodemon server.js --ignore ./scripts/ -unhandled-rejections=strict"
  },
  "author": "Sandeep Baynes",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.0",
    "body-parser": "^1.19.0",
    "config": "^3.3.3",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
